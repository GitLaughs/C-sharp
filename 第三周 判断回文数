 using System;
using System.Linq;

class Program1 {
    static void Main(string[] args) {
        // 从控制台读取一行输入，并将其分割成整数数组
        int[] numbers = Console.ReadLine().Split(' ').Select(int.Parse).ToArray();
        // 找到并输出最大的整数
        Console.WriteLine(numbers.Max());
    }
}
